require(['application',
         'header/languages/app_header',
         'header/currencies/app_header',
         'header/links/app_header',
         ],function(Mystore){

		'use strict';
  
		// var compiledTemplate =templateManager.getTemplate('hello');
		// var html = compiledTemplate({ name : 'World' });
		// alert(html);
  
		Mystore.start();

});